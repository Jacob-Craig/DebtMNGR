# 0003: Smart Cross-Group Settlement Allocation

Balances are maintained per-Group, but dashboard settlements can be triggered globally across all shared groups for a Participant.

When a user initiates a consolidated cross-group settlement, the system automatically allocates the payment into discrete, balanced Settlement Transactions within each underlying Group according to its proportional debt. This keeps group ledgers autonomous and balanced while allowing participants to clear debts with a single payment.
